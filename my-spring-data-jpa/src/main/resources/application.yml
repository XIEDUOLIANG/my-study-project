spring:
  datasource:
    url: jdbc:mysql://localhost:3405/jpa_test
    username: root
    password: my-secret-pw
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true